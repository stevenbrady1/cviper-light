# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Turborepo
.turbo/

# Tauri (generated — never edit, never commit)
**/src-tauri/target/
**/src-tauri/gen/

# Local / environment
*.local
.env
.env.*
!.env.example

# Editors / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Reference material copied verbatim from the PRIVATE production CViper repo
# (backend Python, tests, internal spec). Input for porting only — never ours to
# publish. This repo is mirrored publicly, so committing it would put proprietary
# source into a public history where deletion does not remove it.
docs/ported-from-main/

# App Store screenshots are uploaded, never committed (docs/app-store/SCREENSHOTS.md)
docs/app-store/screenshots/
