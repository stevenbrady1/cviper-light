/**
 * @cviper/job-apis — job board clients (Adzuna, Reed) and keyless search links.
 *
 * PHASE 0: stub only. Clients land in a later phase.
 */
export const JOB_APIS_PACKAGE = '@cviper/job-apis' as const;
