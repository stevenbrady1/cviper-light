/**
 * @cviper/ui — shared presentational components.
 *
 * PHASE 0: stub only. Components land in a later phase. Deliberately free of
 * JSX so the package typechecks before React is wired in.
 */
export const UI_PACKAGE = '@cviper/ui' as const;
